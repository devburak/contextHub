const { PERMISSIONS, PERMISSION_GROUPS } = require('./permissions');
const { ROLE_KEYS, ROLE_LEVELS, DEFAULT_ROLES, getRoleLevel } = require('./roles');

module.exports = {
  PERMISSIONS,
  PERMISSION_GROUPS,
  ROLE_KEYS,
  ROLE_LEVELS,
  DEFAULT_ROLES,
  getRoleLevel
};
