server {
  listen 80;
  listen [::]:80;
  listen 443 ssl http2;
  listen [::]:443 ssl http2;
  ssl_certificate_key /etc/nginx/ssl-certificates/ctxhub.net.key;
  ssl_certificate /etc/nginx/ssl-certificates/ctxhub.net.crt;
  server_name www.ctxhub.net;
  return 301 https://ctxhub.net$request_uri;
}

server {
  listen 80;
  listen [::]:80;
  listen 443 ssl http2;
  listen [::]:443 ssl http2;
  ssl_certificate_key /etc/nginx/ssl-certificates/ctxhub.net.key;
  ssl_certificate /etc/nginx/ssl-certificates/ctxhub.net.crt;
  server_name ctxhub.net www1.ctxhub.net;
  root /home/ctxhub/htdocs/ctxhub.net;

  access_log /home/ctxhub/logs/nginx/access.log main;
  error_log /home/ctxhub/logs/nginx/error.log;

  if ($scheme != "https") {
    rewrite ^ https://$host$uri permanent;
  }

  location ~ /.well-known {
    auth_basic off;
    allow all;
  }

  index index.html;

  location ~* ^/developer-docs/.+\.md$ {
    types {
      text/markdown md;
    }
    default_type text/markdown;
    charset utf-8;
    charset_types text/markdown;
    try_files $uri =404;
  }

  location ~* ^.+\.(css|js|jpg|jpeg|gif|png|ico|gz|svg|svgz|ttf|otf|woff|woff2|eot|mp4|ogg|ogv|webm|webp|zip|swf)$ {
    add_header Access-Control-Allow-Origin "*";
    expires max;
    access_log off;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }

  if (-f $request_filename) {
    break;
  }
}
